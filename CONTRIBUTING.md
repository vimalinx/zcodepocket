# 参与开发

欢迎提交问题和改进。本项目是独立第三方客户端，不隶属于 ZCode 官方；仅连接用户已经授权、由官方远程服务共享的工作区。

## 本地验证

使用 Node.js 22.13+、Bun 1.3.14 和 Expo SDK 57。先阅读根目录 `AGENTS.md` 与 `README.md`，再执行：

```sh
bun install --frozen-lockfile
npm test
npx tsc --noEmit
npm run lint
npm run check:release-source
```

Android 构建步骤见 README。测试使用本地模拟协议，不要求真实二维码、账号、模型额度或手机。不要为了跑测试连接他人的电脑或创建真实会话。

## 提交范围与验收

- 小范围修改，保留用户未提交的工作。不要提交生成的 `android/`、`ios/`、`node_modules/` 或 `builds/`。
- 涉及导航时，测试真实页面的按钮绑定、返回层级与异步结果；单独测试工具函数并不足以证明页面行为。
- 区分源码测试、构建、实机界面、真实官方连接的验证结果。未测试的层次请明确说明。
- 修改有副作用的远程操作时，不得自动重试结果未知的请求；取消本地等待不代表撤销电脑上的操作。
- 按 [SECURITY.md](SECURITY.md) 清理截图和日志，不要上传配对链接、二维码、令牌、对话内容或私人工作区路径。
- 代码贡献沿用本仓库 MIT 许可证，并保留第三方版权声明。不要复制或提交 ZCode 官方网页包、桌面二进制、模型凭据或专有资产。

提交 PR 时说明改动原因、测试命令、平台与未完成的验证。正式发布需按 [RELEASING.md](RELEASING.md) 单独确认。
