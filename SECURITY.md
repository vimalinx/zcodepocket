# 安全与隐私

本项目处于早期开发阶段，尚未接受独立安全审计，也不承诺响应时限。生产或敏感工作区请自行评估风险。

## 报告问题

不要在公开 Issue、PR、截图或日志中提交可用的配对二维码、完整 `remote/v4` 链接、令牌、签名密钥、聊天记录或私人路径。

若仓库 Security 页面提供私密漏洞报告入口，请使用它。否则先开一个不含漏洞细节或凭据的联络 Issue，请维护者提供私密沟通方式。不要把实际可利用的细节或用户数据放入联络 Issue。

## 边界

- 仅操作自己或获授权的电脑和工作区。二维码是访问凭据，不是普通链接。
- 配对凭据使用 Expo SecureStore 保存；连接通过官方 WSS 中转，不宣称由本项目提供端到端加密。
- 默认新会话使用全自动模式，可能减少工具执行确认。只连接可信工作区，并按需要改用其他模式。
- 更新仅在用户确认后打开 GitHub APK 下载地址，Android 负责签名校验；本项目不会静默安装。
- 请求超时可能已经在电脑执行，不能假定失败并自动重试。
- `.gitignore` 不能清除 Git 历史中的秘密。若发生泄露，先撤销或轮换凭据，再决定历史处置；不要直接强推或删除用户历史。

## 发布要求

源码和完整 Git 历史需要分别进行脱敏扫描。扫描无告警不代表没有泄露或没有安全问题。依赖告警与分析见 [docs/release-readiness.md](docs/release-readiness.md)。

默认 Expo 调试签名不作为正式 APK 发布身份。正式 APK 必须通过 `npm run verify:release-apk -- <apk>`；私钥始终留在仓库外，不能提交到 Git、构建日志或发行附件。
